#Adding git commands in my github repository
Git init
git add <fileNam>
git status
git commit -m "write message for the file"
git checkout -b <Branch name which you want create>
git checkout <branchname>
